var decodeString = function(s) {
  if (s === undefined) return '';
  const stack = [];
  for (let i = 0 ; i < s.length; i++) {
    if (s[i] !== ']') {
      stack.push(s[i]);
    } else {
      
    }

  }
};